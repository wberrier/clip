---
# Can generate with compile_commands.json with "bear"
compile-commands:
  working_dir: .

# No difference between release and debug for now
build:
  release:
    working_dir: .
    command: make -j@NUM_CORES_TO_USE@
